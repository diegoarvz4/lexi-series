<h2>Traducción por <%= link_to @translation.translator.username, @translation.translator %></h2>
<div class="episodes-series-container">
  <%= image_tag @translation.episode.series.picture.url, class: 'episodes-series-image ' if @translation.episode.series.picture? %>
  <h5 class="episodes-series"><%= link_to @translation.episode.series.name, @translation.episode.series %></h5> 
  <hr>
</div>
<div>
<span>Episodio <%= @translation.episode.number %></span>
<span>(<%= @translation.status %>)</span>
<div>
  <span><%= @translation.src_lang %> to </span>
  <span><%= @translation.dst_lang %></span>
  <span><%= @translation.runtime %> min.</span>
</div>
</div>
<hr>
<h3>Due date in <%= "#{time_ago_in_words(@translation.duedate)} - #{@translation.duedate.strftime('%a %d %b %Y')}" %> </h3>
<hr>
<h2>Quality Control</h2>
<% if @qc %>
  <span> <%= link_to 'Ver QC', @qc %> realizado por <%= link_to @qc.reviewer.username, @qc.reviewer.username %> </span>
<% else %>
  <% if current_user.admin? %>
    <span><%= link_to 'Agregar QC', new_quality_control_path(id: @translation.id)%></span>
  <% else %>
    <span>No hay QC por el momento</span>
  <% end %>
<% end %>
<hr>
<div>
  <span><%= link_to 'Atrás', request.referrer%><span>
</div>